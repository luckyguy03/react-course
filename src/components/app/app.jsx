import { Layout } from "../layout/layout";
//import { Counter } from "../counter/counter";
import { Tabs } from "../tabs/tabs";

export const App = () => {
  return (
    <Layout>
      <Tabs />
    </Layout>
  );
};
